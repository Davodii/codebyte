<p>
    <strong>Bubble sort</strong> repeatedly steps through an array, compares neighbouring
    elements, and swaps them if they are in the wrong order. After each full pass,
    the largest unsorted element "bubbles" to its correct position at the end.
</p>
<p>
    The code uses two nested loops to compare every pair of neighbours. Right now
    the <code>if</code> block is empty — the comparisons happen but nothing moves.
    Your job is to add the swap so the algorithm actually sorts the array.
</p>
